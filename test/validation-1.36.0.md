# 1.36.0 验证记录

2026-09-08，独立无界面 Chrome：31 张测试页、614 项断言全部通过。
测试资源均来自本地文件，没有访问真实招聘网站。

## 本次诊断修复

- 深度探测开始前冻结完整字段清单，探测引发页面重绘、折叠或节点卸载后仍保留原字段。
- 忽略带 `leave`、`hidden` 或 `aria-hidden` 状态的退场弹层。
- 在点击前记录已有弹层和选项，只把本次点击新出现的内容记入当前控件证据。
- 等待新弹层出现后再采集结构，避免同步打开的日期面板被错当成旧弹层。

## 新增验证

`deep-diagnostics.html` 扩展为 8 项断言，新增覆盖：页面上残留可见的 `slide-up-leave-active` 旧弹层，以及日期探测触发重渲染后被移除的“大赛成绩”字段。旧弹层没有串入日期证据，消失字段仍在最终诊断清单中。

其余 30 张既有测试页全部通过，包括中文月份面板、日期区间、下拉选项、诊断导出和安全边界测试。

## 边界

真实网易页面的动态组件和服务端保存仍需重新加载 1.36.0 后现场核验。
